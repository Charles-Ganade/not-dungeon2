import type { Component } from "solid-js";
import { DeltaPair, WorldState } from "./lib/models/world_state";
import { StoryHistory, StoryTurn } from "./lib/models/story_history";

const App: Component = () => {
    return (
        <p class="text-4xl text-green-700 text-center py-20">Hello tailwind!</p>
    );
};

export default App;
